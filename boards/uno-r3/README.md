# Proposed branch: `uno-r3`

Not on `main`. Uno R3 + KS0258 is a reduced Mega stack: hardware I2C on A4/A5, so only A0–A3 remain for analog limit ladders (four turnouts with feedback).

Snowball Creek still stacks. Do not add the Waveshare 4" TFT on this branch; that is `uno-r3-tft` and drops the KS0258 shield.
